- content_for :breadcrumb do
  %ul.breadcrumb
    %li
      = link_to content_tag(:span, 'Pages'), admin_pages_path
    %li
      = content_tag :span, 'New page', :style => 'color: forestgreen'

- form_for @page, :url => admin_pages_path, :html => { :autocomplete => :off, :multipart => true, :class => :qform } do |f|
  = render :partial => 'form', :locals => { :f => f }
  

%hr
.i_buttons{ :style => 'margin-top: 15px;' }
  = link_to 'Back', admin_pages_path, :class => :arrow_left
