- content_for :breadcrumb do
  %ul.breadcrumb
    %li
      = link_to content_tag(:span, 'Projects'), admin_projects_path
    %li
      = content_tag :span, 'New Project', :style => 'color: forestgreen'

- form_for @project, :url => admin_projects_path, :html => { :autocomplete => :off, :multipart => true, :class => :qform } do |f|
  = render :partial => 'form', :locals => { :f => f }
  

%hr
.i_buttons{ :style => 'margin-top: 15px;' }
  = link_to 'Back', admin_projects_path, :class => :arrow_left
